import React from 'react';
const Read = () => {
  return (
    <div>
      <h2>Componente READ</h2>
      <p>Aquí se mostrará la lista de datos desde la base o API.</p>
    </div>
  );
};// Este componente mostrará los datos guardados en el sistema (la parte de "Read" del CRUD).

export default Read; // Se exporta con "export default" para poder importarlo en App.jsx.